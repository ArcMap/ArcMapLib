const geometryType = String(
  featureLayer.geometryType ?? ""
).toLowerCase();

const placement =
  geometryType.includes("polyline")
    ? "center-along"
    : individualLabelJson.labelPlacement ?? "above-right";
console.log(
   "geometry:",
   featureLayer.geometryType,
   "placement:",
   placement
);
return new LabelClass({
    labelExpressionInfo: {
        expression: safeExpression
    },
    labelPlacement: placement,
    symbol: labelSymbol
});