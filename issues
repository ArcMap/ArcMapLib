await this.graphicsEditor.update([graphic], {
  tool,

  enableRotation: tool === "transform" && !isCircle && this.enableUserEditRotating,
  enableScaling: tool === "transform" && (isCircle || this.enableUserEditScaling),
  preserveAspectRatio: isCircle || this.enableUserEditUniformScaling,

  multipleSelectionEnabled: false,
  toggleToolOnClick: false,
} as any);