const requestedPlacement = individualLabelJson.labelPlacement;

const placement =
  geometryType === "polyline" ||
  geometryType === "esrigeometrypolyline"
    ? requestedPlacement === "above-along" ||
      requestedPlacement === "below-along" ||
      requestedPlacement === "center-along"
      ? requestedPlacement
      : "center-along"
    : requestedPlacement ?? "above-right";