-search';

  constructor(
    private readonly http: HttpClient
  ) {}

  getSmartSearchedData(
    searchedData: string
  ): Observable<SmartSearch> {

    const params = new HttpParams()
      .set('searchedData', searchedData);

    return this.http.get<SmartSearch>(
      this.smartSearchUrl,
      { params }
    );
  }
}