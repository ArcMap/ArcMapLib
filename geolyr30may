if (changedProperties.has('infoTemplate') && this.featureLayer) {
    this.featureLayer.graphics.forEach((graphic: any) => {
        graphic.popupTemplate =
            this._editorOpen || this.inDrawingMode || !this.infoTemplate
                ? (null as any)
                : (this.buildPopupTemplate(graphic) as any);
    });
}
