[...arcgisMap.shadowRoot.querySelectorAll(".content-wrapper")].map((el, i) => ({
  index: i,
  text: el.innerText,
  rect: el.getBoundingClientRect(),
  overflowY: getComputedStyle(el).overflowY,
  background: getComputedStyle(el).backgroundColor
}))