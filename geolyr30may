// stay static — constructed during normal map/layer operation
import PopupTemplate from "@arcgis/core/PopupTemplate";
import Polygon from "@arcgis/core/geometry/Polygon";
import Polyline from "@arcgis/core/geometry/Polyline";

// type-only — erased at compile time, zero bundle cost
import type Zoom from "@arcgis/core/widgets/Zoom";
import type Locate from "@arcgis/core/widgets/Locate";
import type Home from "@arcgis/core/widgets/Home";
import type ScaleBar from "@arcgis/core/widgets/ScaleBar";
import type { ViewMouseWheelEvent } from "@arcgis/core/views/input/types";



const widgetTasks: Promise<void>[] = [];
if (this.showZoom) {
    widgetTasks.push(import("@arcgis/core/widgets/Zoom.js").then(({ default: Zoom }) => {
        this.zoomWidget = new Zoom({ view });
        view.ui.add(this.zoomWidget, "top-left");
    }));
}
// repeat for showHome / showLocate / scale bar
await Promise.all(widgetTasks);
