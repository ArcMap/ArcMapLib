<arc-map id="arcMap">

  <div id="pointContainer">

    <arc-point id="point1"></arc-point>

    <arc-point id="point2"></arc-point>

  </div>

</arc-map>

const container = document.createElement("div");

for (const data of pointData) {

  const point = document.createElement("arc-point");

  point.latitude = data.latitude;
  point.longitude = data.longitude;

  // BAD if connectedCallback executes here
  document.body.appendChild(point);
}

// Moving it later doesn't prevent the earlier error
arcMap.appendChild(container);