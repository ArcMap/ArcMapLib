serializeMapState(): string {
  const state = this._state();

  const displayedLayers = state.activeLayerIds.map((layerId) => ({
    uniqueLayerId: Number(layerId),
    isChecked: true,
  }));

  return JSON.stringify({ displayedLayers });
}
