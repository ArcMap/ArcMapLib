// rollup.config.js
const isProduction = process.env.NODE_ENV === 'production';

export default {
  input: 'src/index.ts',
  external: isProduction
    ? [
        /^@arcgis\/core/,
        /^@arcgis\/map-components/,
        /^@esri\/calcite-components/,
        'lit',
        /^lit\//,
        /^@lit\//,
      ]
    : [
        // ✅ Dev — only keep arcgis external
        /^@arcgis\/core/,
        /^@arcgis\/map-components/,
        /^@esri\/calcite-components/,
      ],
}
