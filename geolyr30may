loadSavedMap(id: string): void {
  console.log('[loadSavedMap] called with id =', id);
  const savedMap = this.savedMaps().find((map) => map.id === id);
  console.log('[loadSavedMap] found map =', savedMap);
  // ... rest
}
