void this.router
  .navigate([], {
    relativeTo: this.activatedRoute,
    queryParams: { /* ... */ },
    queryParamsHandling: 'merge',
    replaceUrl: true,
  })
  .then((committed) => console.log('nav committed?', committed));
