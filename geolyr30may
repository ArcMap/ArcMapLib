const polygonLayer =
  window.upMapDebug.map.layers
    .toArray()
    .find(l =>
      l.id.includes("Polygon Layer") &&
      l.id.includes("graphics")
    );

polygonLayer.graphics.forEach(g => {
  g.symbol.color.a = 1;
});

polygonLayer.refresh?.();