

  if (marker.type === 'work-range-point') {
    return {
      geometry: {
        type: 'point',
        latitude: marker.latitude,
        longitude: marker.longitude,
      },
      symbol: {
        type: 'simple-marker',
        style: 'circle',
        size: 10,
        color: [255, 0, 0, 255],
        outline: {
          color: [255, 0, 0, 255],
          width: 0,
        },
      },
    };
  }

  // KEEP YOUR EXISTING PIN-MARKER CODE BELOW
  return {
    geometry: {
      type: 'point',
      latitude: marker.latitude,
      longitude: marker.longitude,
    },
    symbol: this.getRouteMarkerSymbol(),
    // your existing popupTemplate etc...
  };
}