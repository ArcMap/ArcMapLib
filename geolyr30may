if (setAsDefault) {
  this.addFavoriteMapId(saved.mapID);
  this.userFacade.setPreference('userDefaultSavedMap', saved.mapID);  // ← corrected key

  const mapItem = this.toMapSelectionItem(savedMap);   // reuse the same conversion openMap() uses
  this.layersFacade.setSelectedMapItem(mapItem);
  this.updateMapQueryParams(mapItem);                   // if this method lives on SavedMapFacade; otherwise the panel component's equivalent
}
