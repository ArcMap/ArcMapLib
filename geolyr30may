import path from "path";
import { fileURLToPath } from "url";
import resolve from "@rollup/plugin-node-resolve";
import commonjs from "@rollup/plugin-commonjs";
import typescript from "@rollup/plugin-typescript";
import replace from "@rollup/plugin-replace";
import postcss from "rollup-plugin-postcss";

const __filename = fileURLToPath(import.meta.url);
const __dirname = path.dirname(__filename);

const bundleForDev = process.env.DEV_BUNDLE === "true";

/**
 * IMPORTANT:
 * Do NOT externalize ArcGIS packages.
 * Customer should install only UPMap, so ArcGIS must be resolved from UPMap dependencies.
 */
const peerExternals = [
  /^lit$/,
  /^lit\//,
  /^@up-elements\/core/
];

export default {
  input: path.resolve(__dirname, "src/index.ts"),

  output: {
    dir: bundleForDev ? "./dist-dev" : "./dist",
    format: "es",
    sourcemap: true,
    chunkFileNames: "chunks/[hash].js"
  },

  external: bundleForDev ? [] : peerExternals,

  plugins: [
    resolve({
      browser: true,
      preferBuiltins: false,
      dedupe: ["lit", "@up-elements/core"]
    }),

    commonjs(),

    typescript({
      tsconfig: bundleForDev ? "./tsconfig.dev.json" : "./tsconfig.json"
    }),

    postcss({
      inject: true,
      minimize: true,
      extract: false
    }),

    replace({
      preventAssignment: true,
      "process.env.NODE_ENV": JSON.stringify(
        process.env.NODE_ENV || "development"
      )
    })
  ],

  onwarn(warning, warn) {
    if (warning.code === "CIRCULAR_DEPENDENCY") {
      return;
    }

    warn(warning);
  }
};