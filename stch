const upAuto = document.querySelector('up-map-search')
  ?.shadowRoot
  ?.querySelector('up-autocomplete');

// Check current values:
console.log('items:', upAuto.items);
console.log('template:', upAuto.template);
console.log('formatInput:', upAuto.formatInput);

// Check first item structure:
if(upAuto.items?.length > 0) {
  console.log('item[0]:', upAuto.items[0]);
  console.log('item[0] keys:', 
    Object.keys(upAuto.items[0]));
}
