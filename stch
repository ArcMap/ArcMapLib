jest.mock('lit', () => ({
  LitElement: class LitElement {
    shadowRoot: any = null;
    updateComplete = Promise.resolve(true);
    requestUpdate() {}
    connectedCallback() {}
    disconnectedCallback() {}  // ← add this!
    firstUpdated() {}
    updated() {}
  },
  html: (strings: any, ...values: any[]) =>
    ({ strings, values }),
  css: (strings: any, ...values: any[]) =>
    ({ strings, values }),
}));
