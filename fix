private determineExistingGeometryType(): string | undefined {
  const source = this.featureLayer?.source;
  if (!source || source.length === 0) {
    return undefined;
  }

  const firstGraphic = source.toArray()[0];
  return firstGraphic?.geometry?.type;
}