// Call the details callback to get HTML content
let htmlContent = "";
try {
  htmlContent = this.infoTemplate.details(graphic) ?? "";
  console.log("[LOG 1] details() returned:", htmlContent);   // ← ADD THIS LINE
  console.log("[LOG 1] details() returned length:", htmlContent.length);   // ← AND THIS
} catch (err) {
  console.warn(
    "[up-geojson-layer] infoTemplate.details() failed",
    err,
  );
  return null;
}
