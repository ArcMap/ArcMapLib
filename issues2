<style>
  /* Fix arcgis-popup white shadow - target the actual culprits */
  
  /* Stop flex containers from expanding */
  arcgis-feature::part(container),
  arcgis-feature-content::part(root),
  arcgis-feature-content::part(container),
  arcgis-feature-content::part(main) {
    flex: 0 0 auto !important;
    height: auto !important;
    min-height: 0 !important;
  }
  
  /* Force calcite-flow-item to fit content */
  calcite-flow-item {
    height: auto !important;
    min-height: 0 !important;
  }
  
  calcite-flow-item::part(content-container),
  calcite-flow-item::part(content) {
    flex: 0 0 auto !important;
    height: auto !important;
    min-height: 0 !important;
  }
  
  /* Override the parent calcite-flow */
  calcite-flow {
    height: auto !important;
    min-height: 0 !important;
  }
  
  /* Target arcgis-popup itself */
  arcgis-popup {
    height: auto !important;
    min-height: 0 !important;
  }
</style>
