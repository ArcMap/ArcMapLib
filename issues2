I am developing a LitElement ArcGIS GeoJSON layer library and integrating it into an Angular app. Do not rename existing methods, fields, or public API names.

Important names to preserve:
- eventHandles
- getHitGraphic(...)
- bindViewEvents(...)
- activateGraphicsEditor(...)
- handleEditorDoubleClick(...)
- setMapCursor(...)
- emitLayerEvent(eventName, feature, graphic, extraData)
- showGraphicPopup(...)
- openPopup(...)
- closePopup(...)
- enableInfoPopupWindow(...)

Problem:
When integrated into Angular, clicking a geofence polygon/circle does not show the info template popup. The Angular infoTemplate builder function is being called and HTML is printed in console, but popup is not visible. Cursor also stays default instead of pointer on geofence hover. Popup must appear on single click only. Popup must not appear when editor mode is on. If multiple GeoJSON layers overlap, the top layer should show its popup, not the background layer.

Task:
1. Inspect the current geojson-layer.ts implementation.
2. Fix hitTest logic so getHitGraphic(...) reliably detects polygon/circle fill, not only outline.
3. Ignore labelLayer and sketchLayer in hitTest.
4. Prevent background layer popup when another layer is above it.
5. Fix bindViewEvents(...) without removing existing behavior.
6. Keep single click popup behavior when enableUserEdit is false.
7. Keep single click editor behavior when enableUserEdit is true.
8. Ensure popup is suppressed during editor/drawing mode.
9. Fix popup rendering fully inside the library, without Angular code changes.
10. Do not use UPI/up-map naming in new code.
11. Do not rename existing variables/methods unless absolutely required.
12. Give complete updated function bodies only, not small snippets.

Before editing, explain which functions need changes and why.
Then provide full copy-paste replacements for only those functions.