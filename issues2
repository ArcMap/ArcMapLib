import { css } from "lit";

export const upMapStyles = css`
  :host {
    display: block;
    height: 100%;
    width: 100%;
  }

  arcgis-map {
    width: 100%;
    height: 100%;
    display: block;
    min-height: 500px;
  }

  .map-wrapper {
    height: 100%;
    width: 100%;
    overflow: visible;
  }

  .widget-stack {
    position: absolute;
    left: 16px;
    bottom: 30px;
    display: flex;
    flex-direction: column;
    gap: 12px;
    z-index: 10;
  }

  .widget-stack arcgis-scale-bar {
    position: static;
  }

  /* ✅ Fix arcgis-popup white shadow / empty space issue */
  arcgis-popup {
    --calcite-popup-content-min-height: 0;
    --arcgis-popup-content-min-height: 0;
  }

  arcgis-popup::part(content) {
    min-height: 0 !important;
    height: auto !important;
  }

  arcgis-popup::part(main-container) {
    min-height: 0 !important;
    height: auto !important;
  }

  .esri-popup__main-container {
    min-height: 0 !important;
    height: auto !important;
  }

  .esri-popup__content {
    min-height: 0 !important;
    height: auto !important;
    max-height: 80vh;
    overflow-y: auto;
  }
`;
