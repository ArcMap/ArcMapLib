const attrs = {
  ...(feature.properties ?? {}),
  [this.uniqueIdPropertyName]: 
    feature.properties?.[this.uniqueIdPropertyName] ?? 
    (feature as any).id ?? 
    Date.now(),
  OBJECTID: 
    feature.properties?.[this.uniqueIdPropertyName] ?? 
    feature.properties?.OBJECTID ??
    (feature as any).id ?? 
    Date.now(),
};
