const popupEl =
  typeof mapEl?.getPopupComponent === "function"
    ? mapEl.getPopupComponent()
    : null;

// 👇 ADD THESE DEBUG LOGS
console.log("[DEBUG] mapEl:", mapEl);
console.log("[DEBUG] popupEl:", popupEl);
console.log("[DEBUG] popupTemplate:", popupTemplate);
console.log("[DEBUG] location:", location);

if (popupEl) {
  // ... existing code
}
