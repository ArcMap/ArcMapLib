await this.graphicsEditor.update([graphic], {
  tool: isCircle ? "transform" : "reshape",

  enableRotation: false,

  // Circle resize handles
  enableScaling: isCircle ? true : this.enableUserEditScaling,
  enableUniformScaling: isCircle ? true : this.enableUserEditUniformScaling,

  // Keep for compatibility, but don't rely only on this
  preserveAspectRatio: isCircle ? true : this.enableUserEditUniformScaling,

  multipleSelectionEnabled: false,
  toggleToolOnClick: false,
} as any);



console.log("[arc-geojson-layer] activate editor options", {
  isCircle,
  tool: isCircle ? "transform" : "reshape",
  attributes: graphic.attributes,
});