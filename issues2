private normalizeZoom(value: number | undefined): number {
  if (typeof value !== "number" || Number.isNaN(value)) {
    return UpMap.DEFAULT_ZOOM;
  }

  const roundedZoom = Math.round(value);

  return Math.max(
    this.minZoom,
    Math.min(this.maxZoom, roundedZoom),
  );
}